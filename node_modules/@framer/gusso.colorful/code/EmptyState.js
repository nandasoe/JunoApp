import { toP3 } from './Utils'

const lineWidth = 1
export const baseColor = '255, 36, 123'

export const box = {
  display: 'grid',
  height: '100%',
  gridTemplateColumns: 'repeat(3, 1fr)',
  alignItems: 'center',
  overflow: 'hidden',

  background: toP3(`rgba(${baseColor}, 0.1)`),
  color: toP3(`rgb(${baseColor})`),
  border: lineWidth + 'px solid',
}

export const text = {
  gridColumn: 2,
  fontSize: 12,
  textAlign: 'center',
}

export const arrow = {
  padding: 8,
  fontSize: 15,
  textAlign: 'right',
}
